# Deployment Docs

## Backend
1. Install dependencies: `npm install`
2. Build: `npm run build`
3. Run: `npm start`

## Frontend
1. Install dependencies: `npm install`
2. Build: `npm run build`
3. Serve static output from `frontend/dist`

## Runtime Folders
- uploads/
- logs/
- dist/
